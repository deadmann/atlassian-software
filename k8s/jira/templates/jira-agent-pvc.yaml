apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jira-agent
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: ""

  accessModes:
    - ReadWriteOnce
    
  resources:
    requests:
      storage: 10Mi