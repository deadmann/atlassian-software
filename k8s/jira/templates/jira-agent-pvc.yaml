apiVersion: v1
kind: PersistentVolumeClaim
namespace: {{ .Values.namespace }}
metadata:
  name: jira-agent
spec:
  storageClassName: ""

  accessModes:
    - ReadWriteOnce
    
  resources:
    requests:
      storage: 10Mi