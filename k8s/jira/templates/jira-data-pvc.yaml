apiVersion: v1
kind: PersistentVolumeClaim
namespace: {{ .Values.namespace }}
metadata:
  name: jira-data-pvc
spec:
  storageClassName: ""

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.jira.persistence.size }}